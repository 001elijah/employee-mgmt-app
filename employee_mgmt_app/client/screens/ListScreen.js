//import * as React from "react";
import
{ 
  Text, TextInput, 
  View, Alert, 
  Keyboard, StatusBar 
} from "react-native";

const ListScreen = ({ navigation }) => {
    return(
        <View>
        
        </View>
    );
};

export default ListScreen;
